def eggs(someParameter):
    someParameter.append('hello')

spam = [1,2,3]
eggs(spam)
print(spam)
#list is a mutable data type.list only calls the reference of the data type .
# python does that to save the space and memory and computational thing is important
